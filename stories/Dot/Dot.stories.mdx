import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Views/Dot" />

# Dot

Dispay a dot.

## Examples

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-dot--basic" />
</Canvas>

### Sizes

<Canvas mdxSource={require("!!raw-loader!./Sizes").default}>
  <Story id="examples-dot--sizes" />
</Canvas>

### Intents

<Canvas mdxSource={require("!!raw-loader!./Intents").default}>
  <Story id="examples-dot--intents" />
</Canvas>

### Custom color

<Canvas mdxSource={require("!!raw-loader!./CustomColor").default}>
  <Story id="examples-dot--custom-color" />
</Canvas>

### Animated

<Canvas mdxSource={require("!!raw-loader!./Animated").default}>
  <Story id="examples-dot--animated" />
</Canvas>

## Props

| Property   | Type                                                                                                 | Description                 |
| ---------- | ---------------------------------------------------------------------------------------------------- | --------------------------- |
| `size`     | `null` &#124; `"small"` &#124; `"large"`                                                             | Size                        |
| `intent`   | `null` &#124; `"major"` &#124; `"minor"` &#124; `"positive"` &#124; `"alertive"` &#124; `"negative"` | Intent                      |
| `color`    | `string`                                                                                             | Color                       |
| `animated` | `boolean`                                                                                            | Perform heartbeat animation |

It accepts all props which the native `<span>` element supports.
