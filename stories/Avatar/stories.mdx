import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Views/Avatar" />

# Avatar

Avatar component.

## Examples

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-avatar--basic" />
</Canvas>

### Icon Content

<Canvas mdxSource={require("!!raw-loader!./IconContent").default}>
  <Story id="examples-avatar--icon-content" />
</Canvas>

### Link Avatar

<Canvas mdxSource={require("!!raw-loader!./LinkAvatar").default}>
  <Story id="examples-avatar--link-avatar" />
</Canvas>

### Text Contents

<Canvas mdxSource={require("!!raw-loader!./TextContents").default}>
  <Story id="examples-avatar--text-contents" />
</Canvas>

### Rounded Avatar

<Canvas mdxSource={require("!!raw-loader!./RoundedAvatar").default}>
  <Story id="examples-avatar--rounded-avatar" />
</Canvas>

### Avatar Sizes

<Canvas mdxSource={require("!!raw-loader!./AvatarSizes").default}>
  <Story id="examples-avatar--avatar-sizes" />
</Canvas>

### Custom Size

<Canvas mdxSource={require("!!raw-loader!./CustomSize").default}>
  <Story id="examples-avatar--custom-size" />
</Canvas>

### Custom Color

<Canvas mdxSource={require("!!raw-loader!./CustomColor").default}>
  <Story id="examples-avatar--custom-color" />
</Canvas>

### Avatar Group

<Canvas mdxSource={require("!!raw-loader!./AvatarGroup").default}>
  <Story id="examples-avatar--avatar-group" />
</Canvas>

## API

### Avatar

| Property   | Type                                                             | Description                                                                    |
| ---------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `as`       | `ElementType`                                                    | Element type, default is `"span"`.                                             |
| `size`     | `null` \| `"small"` \| `"large"`                                 | Avatar size.                                                                   |
| `image`    | `string` \| [`ImageProps`](?path=/story/views-image--page#props) | Image url or props of the [`Image`](?path=/story/views-image--page) component. |
| `rounded`  | `boolean`                                                        | Make corners rounded.                                                          |
| `outlined` | `boolean`                                                        | Add outline.                                                                   |

It accepts all props which the `as` element type supports.

### Avatar.Group

| Property   | Type                             | Description           |
| ---------- | -------------------------------- | --------------------- |
| `size`     | `null` \| `"small"` \| `"large"` | Avatar size.          |
| `rounded`  | `boolean`                        | Make corners rounded. |
| `outlined` | `boolean`                        | Add outline.          |

It accepts all props which the native `<div>` element supports.
