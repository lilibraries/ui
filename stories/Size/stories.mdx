import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Configs/Size" />

# Size

This component is used to configure the `size` prop of all subcomponents.

## Examples

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-size--basic" />
</Canvas>

### Nested

The Size component can be nested.

<Canvas mdxSource={require("!!raw-loader!./Nested").default}>
  <Story id="examples-size--nested" />
</Canvas>

## API

### Size

| Property   | Type                             | Description                 |
| ---------- | -------------------------------- | --------------------------- |
| `value`    | `null` \| `"small"` \| `"large"` | **Required** size value.    |
| `children` | `ReactNode`                      | **Required** subcomponents. |

### Size.useConfig

```ts
function useConfig(): null | "small" | "large";
```

Get configured size value.

### Size.Context

A context created by `React.createContext`. It's default value is `null`.
