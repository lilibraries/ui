import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Views/Info" />

# Info

Info component.

## Examples

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-info--basic" />
</Canvas>

### Indicators

<Canvas mdxSource={require("!!raw-loader!./Indicators").default}>
  <Story id="examples-info--indicators" />
</Canvas>

## API

### Info

| Property    | Type        | Description       |
| ----------- | ----------- | ----------------- |
| `children`  | `ReactNode` | Content           |
| `indicator` | `ReactNode` | Specify indicator |

It accepts all props which the native `<div>` element supports.

### Info.Title

It accepts all props which the native `<div>` element supports.

### Info.Detail

It accepts all props which the native `<div>` element supports.
