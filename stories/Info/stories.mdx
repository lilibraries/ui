import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Views/Info" />

# Info

Info component.

## Examples

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-info--basic" />
</Canvas>

### Icons

<Canvas mdxSource={require("!!raw-loader!./Icons").default}>
  <Story id="examples-info--icons" />
</Canvas>

## API

### Info

| Property   | Type        | Description  |
| ---------- | ----------- | ------------ |
| `children` | `ReactNode` | Content      |
| `icon`     | `ReactNode` | Specify icon |

It accepts all props which the native `<div>` element supports.

### Info.Title

It accepts all props which the native `<div>` element supports.

### Info.Detail

It accepts all props which the native `<div>` element supports.
