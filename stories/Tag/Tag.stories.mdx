import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Views/Tag" />

# Tag

Tag component.

## Examples

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-tag--basic" />
</Canvas>

### Clearable

<Canvas mdxSource={require("!!raw-loader!./Clearable").default}>
  <Story id="examples-tag--clearable" />
</Canvas>

### Clickable

This component can be clicked by default. The `clickable` prop only effects component styles, not events.

<Canvas mdxSource={require("!!raw-loader!./Clickable").default}>
  <Story id="examples-tag--clickable" />
</Canvas>

### As link

<Canvas mdxSource={require("!!raw-loader!./AsLink").default}>
  <Story id="examples-tag--as-link" />
</Canvas>

### Sizes

<Canvas mdxSource={require("!!raw-loader!./Sizes").default}>
  <Story id="examples-tag--sizes" />
</Canvas>

### Round

<Canvas mdxSource={require("!!raw-loader!./Round").default}>
  <Story id="examples-tag--round" />
</Canvas>

### Long tag

<Canvas mdxSource={require("!!raw-loader!./LongTag").default}>
  <Story id="examples-tag--long-tag" />
</Canvas>

### Intents

<Canvas mdxSource={require("!!raw-loader!./Intents").default}>
  <Story id="examples-tag--intents" />
</Canvas>

### Solid

<Canvas mdxSource={require("!!raw-loader!./Solid").default}>
  <Story id="examples-tag--solid" />
</Canvas>

### Outlined

<Canvas mdxSource={require("!!raw-loader!./Outlined").default}>
  <Story id="examples-tag--outlined" />
</Canvas>

### Disabled

<Canvas mdxSource={require("!!raw-loader!./Disabled").default}>
  <Story id="examples-tag--disabled" />
</Canvas>

## Props

| Property    | Type                                                                                                 | Description                       |
| ----------- | ---------------------------------------------------------------------------------------------------- | --------------------------------- |
| `children`  | `ReactNode`                                                                                          | Tag label                         |
| `as`        | `ElementType`                                                                                        | Element type, default is `"span"` |
| `variant`   | `null` &#124; `"solid"` &#124; `"outlined"`                                                          | Variant                           |
| `size`      | `null` &#124; `"small"` &#124; `"large"`                                                             | Size                              |
| `intent`    | `null` &#124; `"major"` &#124; `"minor"` &#124; `"positive"` &#124; `"alertive"` &#124; `"negative"` | Intent                            |
| `round`     | `boolean`                                                                                            | Make corners round                |
| `clearable` | `boolean`                                                                                            | Show clear icon                   |
| `clearIcon` | `boolean`                                                                                            | Custom clear icon                 |
| `clickable` | `boolean`                                                                                            | Only effect styles                |
| `disabled`  | `boolean`                                                                                            | Disable clear and click evnets    |
| `onClear`   | `MouseEventHandler`                                                                                  | Clear event                       |
| `onClick`   | `MouseEventHandler`                                                                                  | Click event                       |

It accepts all props which the `as` prop supports.
