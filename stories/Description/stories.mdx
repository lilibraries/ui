import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Views/Description" />

# Description

Description component.

## Examples

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-description--basic" />
</Canvas>

### Icons

<Canvas mdxSource={require("!!raw-loader!./Icons").default}>
  <Story id="examples-description--icons" />
</Canvas>

## API

### Description

| Property   | Type        | Description |
| ---------- | ----------- | ----------- |
| `icon`     | `ReactNode` | Icon.       |
| `children` | `ReactNode` | Content.    |

It accepts all props which the native `<div>` element supports.

### Description.Title

It accepts all props which the native `<div>` element supports.

### Description.Detail

It accepts all props which the native `<div>` element supports.
