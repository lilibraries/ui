import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Forms/Switch" />

# Switch

A boolean control component.

## Examples

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-switch--basic" />
</Canvas>

### Icon

<Canvas mdxSource={require("!!raw-loader!./Icon").default}>
  <Story id="examples-switch--icon" />
</Canvas>

### Labelled

<Canvas mdxSource={require("!!raw-loader!./Labelled").default}>
  <Story id="examples-switch--labelled" />
</Canvas>

### Sizes

<Canvas mdxSource={require("!!raw-loader!./Sizes").default}>
  <Story id="examples-switch--sizes" />
</Canvas>

### Loading

<Canvas mdxSource={require("!!raw-loader!./Loading").default}>
  <Story id="examples-switch--loading" />
</Canvas>

### Disabled

<Canvas mdxSource={require("!!raw-loader!./Disabled").default}>
  <Story id="examples-switch--disabled" />
</Canvas>

### Borderless

<Canvas mdxSource={require("!!raw-loader!./Borderless").default}>
  <Story id="examples-switch--borderless" />
</Canvas>

## Props

| Property         | Type                                     | Description                                  |
| ---------------- | ---------------------------------------- | -------------------------------------------- |
| `size`           | `null` &#124; `"small"` &#124; `"large"` | Size                                         |
| `icon`           | `ReactNode`                              | Slider icon                                  |
| `children`       | `ReactNode`                              | Same as `icon` prop, but with lower priority |
| `checkedLabel`   | `ReactNode`                              | Checked label                                |
| `uncheckedLabel` | `ReactNode`                              | Unchecked label                              |
| `borderless`     | `boolean`                                | Remove borders                               |
| `loading`        | `boolean`                                | Show spinner on the slider                   |
| `loadingIcon`    | `ReactNode`                              | Custom spinner icon                          |
| `loadingDelay`   | `number`                                 | Loading delay time, in milliseconds          |
| `disabled`       | `boolean`                                | Disable change event                         |
| `checked`        | `boolean`                                | Controlled checked value                     |
| `defaultChecked` | `boolean`                                | Uncontrolled checked value                   |
| `onChange`       | `ChangeEventHandler<HTMLInputElement>`   | Change event                                 |
| `inputRef`       | `Ref<HTMLInputElement>`                  | Ref to internal input element                |
| `inputProps`     | `InputHTMLAttributes<HTMLInputElement>`  | Internal input props                         |

It accepts all props which the native `<label>` element supports.
