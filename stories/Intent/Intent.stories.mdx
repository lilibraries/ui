import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Configs/Intent" />

# Intent

This component is used to configure the `intent` prop of all subcomponents.

## Examples

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-intent--basic" />
</Canvas>

### Nested

<Canvas mdxSource={require("!!raw-loader!./Nested").default}>
  <Story id="examples-intent--nested" />
</Canvas>

## API

### Intent

| Property   | Type                                                                             | Description   | Default |
| ---------- | -------------------------------------------------------------------------------- | ------------- | ------- |
| `value`    | `null` \| `"major"` \| `"minor"` \| `"positive"` \| `"alertive"` \| `"negative"` | Intent value  | `null`  |
| `children` | `ReactNode`                                                                      | Subcomponents | -       |

Intent will inherit the upper configured value if you don't specify the `value` prop.

### Intent.useConfig

```ts
function useConfig():
  | null
  | "major"
  | "minor"
  | "positive"
  | "alertive"
  | "negative";
```

Get configured intent value.

### Intent.Context

A context created by `React.createContext`. It's default value is `null`.
