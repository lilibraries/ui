import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Configs/Direction" />

# Direction

Config document or element direction.

## Examples

### Global

The `Direction` button in the document toolbar is used to set the `<html>`'s `dir` attribute, you can click it to try.

```tsx
import React from "react";
import { Direction } from "@lilib/ui";

function Root() {
  return <Direction value="rtl">...</Direction>;
}
```

### Scoped

If the `scoped` prop is specified to `true`, the `children` prop must be a React element that accepts `dir` prop.

<Canvas mdxSource={require("!!raw-loader!./Scoped").default}>
  <Story id="examples-direction--scoped" />
</Canvas>

### Nested

<Canvas mdxSource={require("!!raw-loader!./Nested").default}>
  <Story id="examples-direction--nested" />
</Canvas>

## Props

### Direction

| Property   | Type                          | Description                  | Default |
| ---------- | ----------------------------- | ---------------------------- | ------- |
| `value`    | `null` \| `"ltr"` \| `"rtl"`  | Direction value              | `null`  |
| `scoped`   | `boolean`                     | Scoped                       | `false` |
| `children` | `ReactElement` \| `ReactNode` | Child element or child nodes | -       |

Direction will inherit the upper configured value if you don't specify the `value` prop.

### Direction.useConfig

```ts
function useConfig(): null | "ltr" | "rtl";
```

Get configured direction.

### Direction.Context

A context created by `React.createContext`. It's default value is `null`.
