import { Canvas, Meta, Story } from "@storybook/addon-docs";

<Meta title="Overlays/Tooltip" />

# Tooltip

This component is based on the [Popup](?path=/story/overlays-popup--page) component.

## Examples

More examples are presented in [Popup's document](?path=/story/overlays-popup--page).

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-tooltip--basic" />
</Canvas>

### Link Content

<Canvas mdxSource={require("!!raw-loader!./LinkContent").default}>
  <Story id="examples-tooltip--link-content" />
</Canvas>

### Button Content

<Canvas mdxSource={require("!!raw-loader!./ButtonContent").default}>
  <Story id="examples-tooltip--button-content" />
</Canvas>

## API

The props of the Tooltip component are the same as those of the [Popup](?path=/story/overlays-popup--page#api) component, except that the default value of the `on` prop is `"hover"` and the default value of the `placement` prop is `"top"`.
