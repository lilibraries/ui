import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Overlays/Tooltip" />

# Tooltip

This component is based on the [Popup](?path=/story/overlays-popup--page) component.

## Examples

More examples are presented in [Popup's document](?path=/story/overlays-popup--page).

### Basic

<Canvas mdxSource={require("!!raw-loader!./Basic").default}>
  <Story id="examples-tooltip--basic" />
</Canvas>

### With Link

<Canvas mdxSource={require("!!raw-loader!./WithLink").default}>
  <Story id="examples-tooltip--with-link" />
</Canvas>

### With Buttons

<Canvas mdxSource={require("!!raw-loader!./WithButtons").default}>
  <Story id="examples-tooltip--with-buttons" />
</Canvas>

### Intents

<Canvas mdxSource={require("!!raw-loader!./Intents").default}>
  <Story id="examples-tooltip--intents" />
</Canvas>

## API

| Property | Type                                                                             | Description |
| -------- | -------------------------------------------------------------------------------- | ----------- |
| `intent` | `null` \| `"major"` \| `"minor"` \| `"positive"` \| `"alertive"` \| `"negative"` | Intent      |

More props are presented in [Popup's document](?path=/story/overlays-popup--page#api), except the `on`'s default value is `"hover"` and the `placement`'s default value is `"top"`.
