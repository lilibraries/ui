import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Forms/Radio.Group" />

# Radio.Group

Radio group.

## Examples

### Controlled

<Canvas mdxSource={require("!!raw-loader!./Controlled").default}>
  <Story id="examples-radio-group--controlled" />
</Canvas>

### Uncontrolled

<Canvas mdxSource={require("!!raw-loader!./Uncontrolled").default}>
  <Story id="examples-radio-group--uncontrolled" />
</Canvas>

## Props

| Property       | Type                                   | Description                         |
| -------------- | -------------------------------------- | ----------------------------------- |
| `children`     | `ReactNode`                            | Radios                              |
| `size`         | `null` \| `"small"` \| `"large"`       | Size                                |
| `name`         | `string`                               | Name of all radios                  |
| `value`        | `string`                               | Controlled value                    |
| `defaultValue` | `string`                               | Uncontrolled value                  |
| `loading`      | `boolean`                              | Show spinner                        |
| `loadingIcon`  | `ReactNode`                            | Custom spinner icon                 |
| `loadingDelay` | `number`                               | Loading delay time, in milliseconds |
| `disabled`     | `boolean`                              | Disable all radios                  |
| `onChange`     | `ChangeEventHandler<HTMLInputElement>` | Change event                        |
