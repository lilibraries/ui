import { Meta, Canvas, Story } from "@storybook/addon-docs";

<Meta title="Configs/Baseline" />

# Baseline

## Usage

```tsx
import React from "react";
import { Baseline } from "@lilib/ui";

function App() {
  return <Baseline />;
}
// Or
function App() {
  return <Baseline>...</Baseline>;
}
```

## Examples

### Headings

<Canvas mdxSource={require("!!raw-loader!./Headings").default}>
  <Story id="examples-baseline--headings" />
</Canvas>

### Paragraphs

<Canvas mdxSource={require("!!raw-loader!./Paragraphs").default}>
  <Story id="examples-baseline--paragraphs" />
</Canvas>

### Inline elements

<Canvas mdxSource={require("!!raw-loader!./InlineElements").default}>
  <Story id="examples-baseline--inline-elements" />
</Canvas>

### Horizontal rule

<Canvas mdxSource={require("!!raw-loader!./HorizontalRule").default}>
  <Story id="examples-baseline--horizontal-rule" />
</Canvas>

### Preformatted text

<Canvas mdxSource={require("!!raw-loader!./PreformattedText").default}>
  <Story id="examples-baseline--preformatted-text" />
</Canvas>

### Blockquote

<Canvas mdxSource={require("!!raw-loader!./Blockquote").default}>
  <Story id="examples-baseline--blockquote" />
</Canvas>

### Lists

<Canvas mdxSource={require("!!raw-loader!./Lists").default}>
  <Story id="examples-baseline--lists" />
</Canvas>

### Table

<Canvas mdxSource={require("!!raw-loader!./Table").default}>
  <Story id="examples-baseline--table" />
</Canvas>
